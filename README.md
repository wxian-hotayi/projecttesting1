git init
git add .
git commit -am "initial commit"
git config --global user.email "lee.waixian@hotayi.com"
git config --global user.name "waixian"
git commit -am "initial commit"    
git remote add origin **https://github.com/wxian-hotayi/projecttesting1.git**
git push origin master

git remote -v    // review where the origin to add
